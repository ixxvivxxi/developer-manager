import emoji from 'developer-manager/utils/emoji';
import { module, test } from 'qunit';

module('Unit | Utility | emoji', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = emoji();
    assert.ok(result);
  });
});
